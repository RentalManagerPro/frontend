# Install node and npm
```bash
brew install node
```
## Check if node and npm are installed
```bash
node -v
npm -v
```

# Install dependencies
```bash
npm install
```

# Run the project
```bash
npm run web
```